# CV asset

`Hayden-Farrell-CV.pdf` is the source document linked from the CV page. Keep the
filename and `Profile::cv_download_url` in `src/content.rs` aligned when the CV
is replaced with a newer version.
